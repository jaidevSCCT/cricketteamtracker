ktor:
    application:
        modules:
            - com.arx.ApplicationKt
    deployment:
        port: 9090
        host: 0.0.0.0
    cors:
      allowedOrigins:
        - "localhost:9090"
        - "127.0.0.1:9090"
      allowedMethods:
        - "GET"
        - "POST"
        - "PUT"
        - "DELETE"
      allowedHeaders:
        - "Content-Type"
        - "Authorization"

mongo:
  uri: "mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@cricketdbcluster.q4yjowv.mongodb.net/cricket_db?retryWrites=true&w=majority"
  database: "${MONGO_DATABASE}"
